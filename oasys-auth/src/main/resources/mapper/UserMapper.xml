<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.linter.oasys.auth.dao.UserDao">

    <resultMap type="cn.linter.oasys.auth.entity.User" id="UserMap">
        <result property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="name" column="name"/>
        <result property="sex" column="sex"/>
        <result property="deptName" column="dept_name"/>
        <result property="emailAddress" column="email_address"/>
        <result property="phoneNumber" column="phone_number"/>
        <result property="profilePicture" column="profile_picture"/>
    </resultMap>

    <!--通过ID查询单个用户-->
    <select id="select" resultMap="UserMap">
        select u.id, u.username, u.name, u.sex, u.email_address, u.phone_number, u.profile_picture,
               d.name as dept_name
        from user u, dept d
        where u.id = d.id
          and u.id = #{id}
    </select>

</mapper>